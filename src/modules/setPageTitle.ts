export const setDiaryIndexPageTitle = () => {
  document.title = process.env.REACT_APP_TITLE_DIARY_INDEX || "読み込み失敗";
  return;
};

export const setDiaryShowPageTitle = () => {
  document.title = process.env.REACT_APP_TITLE_DIARY_SHOW || "読み込み失敗";
  return;
};

export const setDiaryEditPageTitle = () => {
  document.title = process.env.REACT_APP_TITLE_DIARY_SHOW || "読み込み失敗";
  return;
};
